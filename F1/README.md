# Crypto Trading с использованием ансамбля моделей и PPO 🚀

## 1. Цель проекта

Разработать систему торговли криптовалютой (BTC/USDT) с использованием ансамбля нейронных сетей для предсказания направления движения цены и опционального Deep Reinforcement Learning агента (PPO) для принятия торговых решений. Проект использует PyTorch Lightning для обучения моделей и ClearML для логирования экспериментов.

### Основные компоненты:

- **Ансамбль моделей**: LSTM, GRU, 1D CNN, MLP для предсказания направления цены
- **PPO агент** (опционально): Reinforcement Learning агент для оптимизации торговых стратегий
- **PyTorch Lightning**: Все модели реализованы как LightningModule
- **ClearML**: Интеграция для логирования экспериментов и метрик
- **Backtesting**: Система бэктестинга для оценки производительности стратегий

## 2. Требования к данным

### Источник данных

Данные загружаются через библиотеку CCXT с биржи Binance в реальном времени. Проект поддерживает различные таймфреймы (1s, 1m, 5m и т.д.).

### Предобработка данных

Система автоматически выполняет:

1. **Загрузка OHLCV данных** с биржи Binance
2. **Вычисление технических индикаторов**:
   - RSI (Relative Strength Index)
   - MACD (Moving Average Convergence Divergence)
   - Bollinger Bands
   - Moving Averages (SMA, EMA)
   - И другие технические индикаторы
3. **Нормализация данных** для обучения моделей
4. **Создание последовательностей** (sequences) для временных рядов
5. **Разделение на train/validation/test** наборы

### Сохранение и загрузка данных

Данные могут быть сохранены в CSV файл для последующего использования:

- Сохранение: `--save-timeseries` флаг
- Загрузка: `--load-timeseries` флаг + путь к файлу через `--timeseries-path`

## 3. Требования к модели

### Тип моделей

Все модели реализованы с использованием **PyTorch Lightning**:

1. **LSTMPredictor** (`pl.LightningModule`): LSTM сеть для предсказания направления цены
2. **GRUPredictor** (`pl.LightningModule`): GRU сеть
3. **CNNPredictor** (`pl.LightningModule`): 1D Convolutional Neural Network
4. **MLPPredictor** (`pl.LightningModule`): Multi-Layer Perceptron
5. **EnsembleModel**: Объединение предсказаний всех моделей
6. **PPOAgent** (`pl.LightningModule`, опционально): Proximal Policy Optimization агент

### Метрики качества

- **Accuracy**: Точность классификации направления цены
- **Loss**: Cross-entropy loss для обучения
- **Trading Metrics**: Sharpe ratio, максимальная просадка, общая доходность при бэктестинге

### Логирование в ClearML

Все эксперименты автоматически логируются в ClearML:
- Гиперпараметры моделей
- Метрики обучения и валидации
- Визуализации и графики
- Сохраненные модели

## 4. Структура репозитория

```
F1/
├── .env                    # Файл с переменными окружения (создать из .env.example)
├── .env.example           # Пример файла с переменными окружения
├── .gitignore             # Файл с правилами игнорирования для Git
├── Makefile              # Файл с командами для автоматизации
├── README.md             # Этот файл
├── pyproject.toml        # Конфигурация проекта Python и зависимости
├── requirements.txt      # Зависимости проекта (альтернативный формат)
├── setup.cfg            # Конфигурация для flake8
├── uv.lock              # Lock файл для uv
├── data/                 # Директория для данных (не используется, данные в timeseries/)
├── timeseries/           # Директория для временных рядов
│   ├── raw/             # Сырые данные (CSV файлы)
│   ├── processed/       # Обработанные данные и предсказания
├── docs/                 # Документация проекта
│   ├── README.md        # Подробная документация
│   └── TRADING_SETUP.md # Инструкции по настройке торговли
├── models/               # Сохраненные модели
│   ├── lstm_model.pt    # Веса LSTM модели
│   ├── gru_model.pt     # Веса GRU модели
│   ├── cnn_model.pt     # Веса CNN модели
│   ├── mlp_model.pt     # Веса MLP модели
│   └── ppo_model.pt     # Веса PPO агента (опционально)
├── logs/                 # Логи обучения и торговли
├── reports/              # Отчеты и визуализации
│   ├── backtest/        # Результаты бэктестинга
│   └── figures/         # Графика для отчетов
├── references/          # Справочные материалы
└── src/                 # Исходный код
    ├── __init__.py
    ├── main.py          # Главная точка входа
    ├── predict.py       # Скрипт для предсказаний
    ├── config.py        # Конфигурационные параметры
    ├── cli/             # Модуль командной строки
    │   └── __init__.py  # Парсинг аргументов командной строки
    ├── timeseries/      # Работа с временными рядами
    │   ├── dataset.py   # Загрузка и предобработка данных
    │   └── features.py  # Извлечение признаков
    ├── modeling/        # Модули для обучения и предсказания
    │   ├── train.py     # Обучение моделей
    │   ├── predict.py   # Функции предсказания
    │   ├── prediction.py # Предсказания с ансамблем
    │   ├── models.py    # Определения моделей (LSTM, GRU, CNN, MLP)
    │   ├── ensemble.py  # Ансамбль моделей
    │   ├── ppo.py       # PPO агент
    │   └── env.py       # Торговая среда (Gymnasium)
    └── eval/            # Оценка моделей
        ├── backtest.py  # Бэктестинг
        └── plots.py     # Визуализации
```

## 5. Требования к train.py

### Функциональность

Скрипт `src/main.py` (который вызывает `src/modeling/train.py`) выполняет:

1. **Загрузка и предобработка данных** через `src/timeseries/dataset.py`
2. **Инициализация моделей** (LSTM, GRU, CNN, MLP) как PyTorch Lightning модули
3. **Обучение моделей** с использованием PyTorch Lightning Trainer
4. **Логирование в ClearML** всех метрик и гиперпараметров
5. **Создание ансамбля** из обученных моделей
6. **Опциональное обучение PPO агента** для продвинутых стратегий
7. **Сохранение весов моделей** в указанные пути
8. **Вывод метрик** на тестовом наборе в консоль
9. **Бэктестинг** обученных моделей

### Параметры командной строки

#### Параметры данных

- `--symbol` (str, default: 'BTC/USDT'): Торговая пара
- `--timeframe` (str, default: '1s'): Таймфрейм данных (1s, 1m, 5m, 1h и т.д.)
- `--days` (int, default: 1): Количество дней данных для загрузки
- `--seq-length` (int, default: 60): Длина последовательности для моделей временных рядов
- `--timeseries-path` (str, default: 'timeseries/raw/btc_usdt_1s.csv'): Путь для сохранения/загрузки данных
- `--load-timeseries` (flag): Загрузить данные из файла, если он существует
- `--save-timeseries` (flag): Сохранить загруженные данные в файл

#### Параметры обучения

- `--batch-size` (int, default: 64): Размер батча для обучения
- `--max-epochs` (int, default: 50): Максимальное количество эпох обучения
- `--n-episodes` (int, default: 100): Количество эпизодов для обучения PPO агента (если используется)
- `--max-steps` (int, default: 1000): Максимальное количество шагов в эпизоде для PPO
- `--ensemble-method` (str, default: 'weighted_average'): Метод объединения предсказаний ('average', 'weighted_average', 'voting')
- `--use-ppo` (flag): Использовать PPO агента для торговли (по умолчанию используется ансамбль моделей напрямую)

#### Параметры ClearML

- `--use-clearml` (flag): Использовать ClearML для логирования (включено по умолчанию в коде)
- `--project-name` (str, default: 'CryptoTrading'): Название проекта в ClearML
- `--task-name` (str, default: 'PPO_BTC_USDT'): Название задачи в ClearML

#### Параметры сохранения моделей

- `--lstm-models-path` (str, default: 'lstm_model.pt'): Путь для сохранения LSTM модели
- `--gru-models-path` (str, default: 'gru_model.pt'): Путь для сохранения GRU модели
- `--cnn-models-path` (str, default: 'cnn_model.pt'): Путь для сохранения CNN модели
- `--mlp-models-path` (str, default: 'mlp_model.pt'): Путь для сохранения MLP модели
- `--agent-models-path` (str, default: 'ppo_agent.pt'): Путь для сохранения PPO агента

#### Параметры бэктестинга

- `--cash` (float, default: 10000): Начальный капитал для бэктестинга
- `--commission` (float, default: 0.002): Комиссия за сделку (0.2%)
- `--threshold` (float, default: 0.55): Порог вероятности для сигналов ансамбля моделей
- `--backtest-only` (flag): Запустить только бэктестинг без обучения (требуются предобученные модели)

### Примеры запуска

#### Базовое обучение моделей

```bash
# Обучение с использованием ансамбля моделей (без PPO)
PYTHONPATH=$(pwd) uv run src/main.py \
    --timeseries-path ./timeseries/raw/btc_usdt_1s.csv \
    --batch-size 64 \
    --max-epochs 50 \
    --seq-length 60 \
    --days 1 \
    --save-timeseries \
    --lstm-models-path ./models/lstm_model.pt \
    --gru-models-path ./models/gru_model.pt \
    --cnn-models-path ./models/cnn_model.pt \
    --mlp-models-path ./models/mlp_model.pt
```

#### Обучение с PPO агентом

```bash
# Обучение с PPO агентом
PYTHONPATH=$(pwd) uv run src/main.py \
    --timeseries-path ./timeseries/raw/btc_usdt_1s.csv \
    --batch-size 64 \
    --max-epochs 50 \
    --n-episodes 100 \
    --use-ppo \
    --lstm-models-path ./models/lstm_model.pt \
    --gru-models-path ./models/gru_model.pt \
    --cnn-models-path ./models/cnn_model.pt \
    --mlp-models-path ./models/mlp_model.pt \
    --agent-models-path ./models/ppo_agent.pt
```

#### Обучение с загрузкой сохраненных данных

```bash
# Загрузка данных из файла (быстрее для повторных экспериментов)
PYTHONPATH=$(pwd) uv run src/main.py \
    --timeseries-path ./timeseries/raw/btc_usdt_1s.csv \
    --load-timeseries \
    --batch-size 64 \
    --max-epochs 50 \
    --lstm-models-path ./models/lstm_model.pt \
    --gru-models-path ./models/gru_model.pt \
    --cnn-models-path ./models/cnn_model.pt \
    --mlp-models-path ./models/mlp_model.pt
```

#### Использование Makefile

```bash
# Обучение через Makefile (рекомендуется)
make train

# Обучение с PPO
make train-ppo

# Быстрое обучение для тестирования (1 эпоха)
make train-quick
```

## 6. Требования к predict.py

### Функциональность

Скрипт `src/predict.py` выполняет:

1. **Загрузка предобученных моделей** из указанных путей
2. **Загрузка данных** для предсказания (из файла или загрузка с биржи)
3. **Создание ансамбля** из загруженных моделей
4. **Генерация предсказаний** и торговых сигналов
5. **Сохранение результатов** в CSV файл с колонками:
   - Путь к данным (timestamp/index)
   - Предсказание (направление: 0 или 1)
   - Вероятность (confidence)
   - Торговые сигналы (buy/sell/hold)
   - Дополнительные метрики

### Параметры командной строки

Скрипт использует те же параметры, что и `train.py`, через общий парсер `src/cli/__init__.py`:

#### Обязательные параметры

- `--timeseries-path` (str): Путь к данным для предсказания или путь для загрузки с биржи
- `--lstm-models-path` (str): Путь к обученной LSTM модели (обязательно)
- `--gru-models-path` (str): Путь к обученной GRU модели (обязательно)
- `--cnn-models-path` (str): Путь к обученной CNN модели (обязательно)
- `--mlp-models-path` (str): Путь к обученной MLP модели (обязательно)

#### Опциональные параметры

- `--symbol` (str, default: 'BTC/USDT'): Торговая пара
- `--timeframe` (str, default: '1s'): Таймфрейм данных
- `--days` (int, default: 1): Количество дней данных
- `--seq-length` (int, default: 60): Длина последовательности
- `--load-timeseries` (flag): Загрузить данные из файла
- `--threshold` (float, default: 0.55): Порог для генерации сигналов
- `--ensemble-method` (str, default: 'weighted_average'): Метод ансамбля

### Формат выходного CSV файла

Результаты сохраняются в `timeseries/processed/{symbol}_{timeframe}_predictions.csv`:

- `timestamp`: Временная метка
- `close`: Цена закрытия
- `prediction`: Предсказание направления (0 - вниз, 1 - вверх)
- `probability`: Вероятность предсказания
- `Signal`: Торговый сигнал (-1 - продать, 0 - держать, 1 - купить)
- Дополнительные технические индикаторы

### Примеры запуска

#### Предсказание с загрузкой данных из файла

```bash
PYTHONPATH=$(pwd) uv run src/predict.py \
    --timeseries-path ./timeseries/raw/btc_usdt_1s.csv \
    --load-timeseries \
    --lstm-models-path ./models/lstm_model.pt \
    --gru-models-path ./models/gru_model.pt \
    --cnn-models-path ./models/cnn_model.pt \
    --mlp-models-path ./models/mlp_model.pt \
    --threshold 0.55
```

#### Предсказание с загрузкой данных с биржи

```bash
PYTHONPATH=$(pwd) uv run src/predict.py \
    --symbol BTC/USDT \
    --timeframe 1s \
    --days 1 \
    --seq-length 60 \
    --lstm-models-path ./models/lstm_model.pt \
    --gru-models-path ./models/gru_model.pt \
    --cnn-models-path ./models/cnn_model.pt \
    --mlp-models-path ./models/mlp_model.pt
```

#### Использование Makefile

```bash
# Предсказание через Makefile
make predict
```

## 7. Общие требования

### Логирование в ClearML

Все эксперименты автоматически логируются в ClearML при запуске `train.py`. Для работы необходимо:

1. Настроить ClearML credentials (см. раздел "Установка и настройка")
2. Убедиться, что флаг `--use-clearml` включен (включен по умолчанию в коде)

ClearML автоматически логирует:
- Все гиперпараметры моделей
- Метрики обучения (loss, accuracy)
- Метрики валидации
- Метрики тестирования
- Визуализации и графики
- Сохраненные модели

### Качество кода

Код соответствует PEP 8 стандартам:
- Используется `setup.cfg` для настройки flake8
- Все функции содержат docstrings
- Типизация параметров в docstrings

### Документация

- Все модули содержат docstrings
- README.md с подробными инструкциями
- Дополнительная документация в `docs/README.md`

### Обработка ошибок

Проект включает обработку ошибок:
- Проверка существования файлов моделей
- Валидация входных данных
- Обработка ошибок загрузки данных с биржи
- Graceful degradation при отсутствии некоторых моделей

## 8. Формат сдачи

### Требования к репозиторию

Минимум необходимых файлов:

1. ✅ `src/modeling/train.py` - код обучения модели
2. ✅ `src/predict.py` - код предсказания модели
3. ✅ `src/main.py` - главная точка входа (вызывает train.py)
4. ✅ Веса моделей (в папке `models/`)
5. ✅ `.env.example` - файл с примерами переменных окружения
6. ✅ `README.md` - этот файл с инструкциями

### Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
# Отредактируйте .env и заполните ваши ClearML credentials
```

### Ссылка на ClearML

После обучения моделей предоставьте ссылку на проект в ClearML:

1. Войдите в ClearML web интерфейс
2. Найдите проект "CryptoTrading" (или указанное вами название)
3. Скопируйте ссылку на проект или конкретную задачу

## Установка и настройка

### Требования

- Python 3.12+
- [uv](https://github.com/astral-sh/uv) - менеджер пакетов Python
- Аккаунт ClearML (бесплатный доступ доступен)

### Установка зависимостей

```bash
# Установка uv (если еще не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Клонирование репозитория (если нужно)
# git clone <repository-url>
# cd F1

# Установка зависимостей
uv sync
```

### Настройка ClearML

#### Вариант 1: Через файл .env (рекомендуется)

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Отредактируйте `.env` и заполните ваши credentials:
   ```env
   CLEARML_API_ACCESS_KEY=your_actual_access_key_here
   CLEARML_API_SECRET_KEY=your_actual_secret_key_here
   CLEARML_WEB_HOST=https://app.clear.ml
   CLEARML_FILES_HOST=https://files.clear.ml
   ```

3. Получите credentials:
   - Зарегистрируйтесь на [https://app.clear.ml](https://app.clear.ml)
   - Перейдите в Settings → Workspace → Create new credentials
   - Скопируйте Access Key и Secret Key

#### Вариант 2: Через clearml-init

```bash
# Установка ClearML CLI (если нужно)
pip install clearml

# Инициализация
clearml-init
```

ClearML автоматически создаст файл конфигурации `~/clearml.conf`.

### Проверка установки

```bash
# Проверка, что все зависимости установлены
uv run python -c "import clearml; import pytorch_lightning; print('OK')"

# Быстрый тест обучения (1 эпоха)
make train-quick
```

## Дополнительная документация

Для более подробной информации см.:
- `docs/README.md` - подробная документация по проекту
- `docs/TRADING_SETUP.md` - инструкции по настройке торговли

## Лицензия

Проект создан в учебных целях.

